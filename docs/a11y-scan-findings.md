# Accessibility scan findings (issue #240)

Findings-only audit against WCAG 2.1 AA. Automated scan tool: `e2e/a11y-scan.ts` (axe-core via Playwright, tags `wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`), run against 24 public routes and 42 portal routes (signed in as `admin@example.test`) on top of `development` as of 2026-08-27. Raw output: `e2e/a11y-report.json` (gitignored — regenerate with `bun run e2e/a11y-scan.ts` against a running `bun run dev` + seeded local Supabase stack; on a slow/synced filesystem, `curl` each route once first to force Turbopack to precompile it, or the scan's 30s per-page timeout can trip on first-compile).

Fixing findings is out of scope for this ticket; each finding below has its own follow-up issue.

## Automated scan results

66 routes scanned, 17 with violations, 0 errors, reducing to two systemic root causes:

| #   | Finding                                                                                                                                                                                                                            | WCAG                          | Severity | Routes                                                                                                                                                                                                                                                                                                                                                                                                              | Follow-up                                                         |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 1   | `Select`/combobox triggers render their current value as visible text, but `role="combobox"` doesn't derive its accessible name from content — screen readers announce only the value with no label                                | 4.1.2 Name, Role, Value (A)   | Critical | `/events/community`; `/gears`, `/gears/library`; `/portal/governance` (+ `meetings`, `board-members`, `resolutions`, `nonprofit-status` — 40 nodes); `/portal/finance/reimbursements`, `/portal/finance/revenue`; `/portal/administration`, `/portal/administration/users`, `/portal/administration/permissions` (140 nodes — a role×resource permission matrix, largest single instance); `/portal/communications` | [#288](https://github.com/chattersnow/chattersnow-web/issues/288) |
| 2   | Gear catalog card (`role="button"`, opens item detail) wraps a focusable add/remove-cart `Checkbox` — nested interactive controls                                                                                                  | 4.1.2 / 2.1.1 Keyboard (A)    | Serious  | `/gears`, `/gears/library`                                                                                                                                                                                                                                                                                                                                                                                          | [#289](https://github.com/chattersnow/chattersnow-web/issues/289) |
| 3   | Two shared `Badge` variants fail contrast at the design-token level: `destructive` (`bg-destructive/10 text-destructive`, 3.37:1) and `secondary` (`bg-secondary text-secondary-foreground`, 3.84:1), both below the 4.5:1 minimum | 1.4.3 Contrast (Minimum) (AA) | Serious  | destructive: `/portal/calendar`, `/portal/calendar/work-queue`, `/portal/administration/audit-log` (23 instances); secondary: `/portal/administration`, `/portal/administration/users`                                                                                                                                                                                                                              | [#290](https://github.com/chattersnow/chattersnow-web/issues/290) |

Both findings are token/shared-component level, so each is likely to reproduce on routes beyond the ones the scan happened to hit — see each follow-up issue for detail.

## Manual spot-check

Keyboard-only tab-order and focus-visibility spot-checks on `/gears` and `/portal/governance/nonprofit-status` (heaviest finding-1 page): tab order is sequential and matches visual layout, all shadcn-based controls show a visible focus ring (`focus-visible:ring-3`), and the nested-interactive card/checkbox pair from finding 2 is reachable via keyboard (not a keyboard trap) — the defect is purely in the accessibility-tree semantics, not keyboard operability. No additional issues found beyond the findings above. A full manual screen-reader pass (VoiceOver/NVDA) across all routes was not performed in this session; the findings above are well-understood enough from the automated results and source inspection that a targeted re-listen isn't needed to file them, but a broader manual pass is still worth doing separately if deeper coverage is wanted.

## Next steps

Per the acceptance criteria: findings are triaged into the two follow-up issues above, each scoped to a single root cause/shared component so they can be picked up independently. Both are worth prioritizing since a single fix (in `select.tsx`'s usages, or the two badge tokens) resolves every listed route at once.

## Re-sweep (2026-08-29)

Re-ran the same scan on top of `development` after #288/#289/#290 (and #421/#424, which added the Access Management module) landed, from a CI run of [#435](https://github.com/chattersnow/chattersnow-web/pull/435) — 25 public routes and 43 portal routes (2 more than the original sweep: `/get-involved/volunteer/status` and `/portal/administration/access-management`, both added to the app since 2026-08-27).

**68 routes scanned, 3 with violations, 0 errors** — down from 17/66. All three findings above are gone (no `combobox` name/role/value violations, no nested-interactive gear card, no `secondary` badge contrast). The remaining 3 are all the same `color-contrast` (serious, AA) rule, 1 node each, on `/portal/administration`, `/portal/administration/users`, and `/portal/administration/system-settings` — filed as [#436](https://github.com/chattersnow/chattersnow-web/issues/436), since it isn't clear from this triage alone whether it's the `destructive` badge token from #290 still sitting right at the 4.5:1 boundary, or a different element on `/system-settings`. That environment had no Docker/local Supabase available, so this pass read CI job logs rather than driving a live scan or downloading `a11y-report.json` directly — see #436 for what's still unconfirmed.

## Keyboard repro pass (2026-09-04, issue #567)

Issue #567 was reported as "dropdown selects are not keyboard-operable", with the repro pass left open: is it every `Select`, or specific screens? The answer turned out to be neither.

**The shared `Select` is fine.** `src/components/ui/select.tsx` is upstream shadcn `base-nova` over Base UI 1.7.0, with one local addition (`collectSelectItems`, which derives the `items` prop from children so `SelectValue` can resolve a label). It has no `onKeyDown`, `tabIndex`, `asChild` or `preventDefault` anywhere, the trigger is a real `<button>`, and Base UI runs `useListNavigation` without `virtual` — so items take real DOM focus and typeahead works. Driven from the keyboard it opens on Enter, moves on the arrow keys, selects on Enter, closes on Escape, and returns focus to its trigger, inside a Dialog as well as outside one. The 23 native `<select>` elements in the server-rendered filter forms are, of course, also fine.

**`PersonPicker` was the real defect.** `src/app/portal/(app)/people/person-picker.tsx` was an `<Input>` followed by a flat list of `<button>`s, reached from 42 files. No `role="combobox"`, no `role="listbox"`/`option`, no `aria-expanded`/`aria-controls`/`aria-activedescendant`, no arrow keys, no Escape, no accessible name on the input (the `FieldLabel`s around it carry no `htmlFor`), and no announcement when a person was picked. Focus also dropped to `<body>` whenever the field swapped for the selected chip or back. It is now built on Base UI's `Autocomplete`, in normal document flow so it stays inside the focus trap of the dialog or sheet that holds it.

**Why nothing caught it.** axe cannot see keyboard operability, and before this the repo held a total of five keyboard assertions, every one of them an Escape press. `src/components/ui/select.dom.test.tsx`, the `keyboard` block in `person-picker.dom.test.tsx`, and the palette's arrow-key test are the guard against the next regression.

**Still open.** The `<FieldLabel htmlFor>` sweep for picker call sites (the `aria-label` fallback covers them for now), and [#659](https://github.com/chattersnow/chattersnow-web/issues/659) / [#663](https://github.com/chattersnow/chattersnow-web/issues/663), which cover the wider manual keyboard and screen-reader pass.
