import { redirect } from "next/navigation";

/** Moved with the rest of the section in #943. */
export default function AccessManagementServicesMovedPage() {
  redirect("/portal/technology/services");
}
