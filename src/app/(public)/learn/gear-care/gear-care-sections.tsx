import { LearnArticleSections } from "../learn-section";
import { GEAR_CARE_ARTICLES } from "./gear-care-data";

export function GearCareSections() {
  return <LearnArticleSections articles={GEAR_CARE_ARTICLES} />;
}
